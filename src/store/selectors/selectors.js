export const selectProducts = (state) => state.productsState.products;
export const selectProduct = (state) => state.productsState.product;
export const selectSizes = (state) => state.productsState.sizes;

export const selectIsLoading = (state) => state.appState.isLoading;
